<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/amedias1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/amedias1"
        android:supportsRtl="true"
        android:theme="@style/Theme.AMedias"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />

        <activity android:name=".LoginActivity" />

        <activity android:name=".CrearGrupoActivity" />

        <activity android:name=".GrupoAMedias" />

        <activity android:name=".UnirseGrupoActivity" />

        <activity android:name=".BalanceActivity" />

        <activity android:name=".ImagenesActivity" />

        <activity android:name=".FullscreenImageActivity" />

    </application>

</manifest>